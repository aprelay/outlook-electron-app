# outlook-electron-app

## Outlook cookie debugger

The Cloudflare Worker in `worker/` starts an Entra device-code flow and reports
safe cookie metadata from controlled `outlook.office365.com` session probes.
See `worker/README.md` for configuration and deployment instructions.
